# NutriAware — Data Quality Report & Cleaning Plan
# تقرير جودة البيانات وخطة التنظيف

## A. Required Inputs / المدخلات المطلوبة
لإجراء تطبيق دقيق 100%، أحتاج إلى (كحد أدنى):
1. **عينة من البيانات (رابط أو ملف CSV/JSON)** أو المخطط النهائي (Schema) المعكوس من Firestore.
2. **شريط القيم المسموحة (Allowed Values)** للحقول الصريحة إن وجدت اختلافات عن التوقعات.
3. **العدد المستهدف للبيانات الجديدة** (مثلاً 100 ألف صف).

*الافتراضات الحالية (لتجهيز السكربتات المرفقة):*
* أن الاستبيان يحتوي على `demographics` (العمر، التعليم، نوع القرابة)، `healthIndicators` (الجنس، مصدر المعلومات)، ومقاييس (1-5 لمعرفة وممارسات، 0-10 للـ NPS).*

---

## B. Data Quality Report / تقرير جودة البيانات

### Top Issues Detected (Assumed from typical seeded data) / أبرز المشاكل المتوقعة:
1. **تكرار الأنماط (Near-duplicates):** وجود مئات الصفوف بنفس تركيبة الديموغرافيا (مثال: أم + 30 سنة + تعليم جامعي) مع إجابات متطابقة للمقاييس.
2. **انعدام التباين (Low Entropy):** حقول الإجابات المفتوحة (مثل `challenges`) تحتوي على نفس 3-4 جمل معادة بشكل غير واقعي.
3. **التناقضات المنطقية (Logical Constraints):** وجود إجابات مثل `relationship = أب` وتحديد `gender = أنثى` (إذا كان القصد بـ gender هو جنس ولي الأمر وليس الطفل).
4. **القيم المفقودة (Missingness):** اختفاء بيانات جوهرية مثل `healthIndicators` بسبب أخطاء في السكربت القديم (Phantom bugs).
5. **الطوابع الزمنية (Clustered Timestamps):** 100% من البيانات تم إنشاؤها في نفس اليوم/الساعة مما يكسر واقعية التحليل الزمني.

### Severity Ranking / ترتيب الخطورة:
- 🔴 **High / عالي:** التناقضات المنطقية (تبطل التحليل الإحصائي المتقاطع).
- 🔴 **High / عالي:** غياب القيم الإجبارية (Missing Required Fields).
- 🟠 **Medium / متوسط:** انعدام التنوع في الإجابات المفتوحة (يمنع اختبار نماذج الـ NLP / Sentiment Analysis).
- 🟡 **Low / منخفض:** تركز الطوابع الزمنية.

---

## C. Cleaning Plan / خطة التنظيف (ETL)
لتنظيف البيانات الحالية (باستخدام البايثون المرفق `clean_survey_data.py`):
1. إسقاط (Drop) أي صفوف فارغة بالكامل (All-Nulls) أو قيم مفقودة في الحقول الحرجة.
2. تطبيق دالة **Levenshtein/Jaccard** لإيجاد وحذف النصوص المكررة في أسئلة (Open-ended).
3. معالجة (Imputation) للقيم المفقودة بالتوزيع الاحتمالي بدلاً من المتوسط (لتجنب الانحياز).
4. توحيد السلاسل النصية (استبدال "ام", "أم ", "Mother" بـ "أم").
5. حذف أي صف فيه تناقضات برمجية (Relationship vs Gender).
6. تصفية التقييمات خارج النطاق (Likert ليس بين 1-5، NPS ليس 0-10).

*(سيقوم السكربت بإخراج 3 ملفات: البيانت النظيفة، تقرير المرفوضات، وتقييم الجودة النهائي).*
