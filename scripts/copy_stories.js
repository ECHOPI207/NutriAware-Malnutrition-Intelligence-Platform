const fs = require('fs'); const path = require('path'); const srcDir = path.join(__dirname, '..', '?????'); const destDir = path.join(__dirname, '..', 'public', 'stories'); if (!fs.existsSync(destDir)) { fs.mkdirSync(destDir, { recursive: true }); } try { fs.copyFileSync(path.join(srcDir, '?? ???? ???? ???? 1.PDF'), path.join(destDir, '?? ???? ???? ???? 1.PDF')); fs.copyFileSync(path.join(srcDir, '?? ? ????? ???? ??????1.PDF'), path.join(destDir, '?? ? ????? ???? ??????1.PDF')); console.log('success'); } catch(e) { console.error(e); }
